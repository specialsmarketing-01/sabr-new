# Public site URL (used for canonical, sitemap, Open Graph; default: https://sabr.at)
NEXT_PUBLIC_SITE_URL=https://sabr.at

# Optional: Google Search Console verification meta tag value
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-verification-code

# Contact form – recipient and SMTP (required for /api/contact to work)
CONTACT_EMAIL=your-email@example.com
SMTP_FROM=noreply@yourdomain.com

# SMTP (e.g. Gmail, SendGrid, or your provider)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
